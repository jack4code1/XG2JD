# 秒杀系统故障演练报告

## 演练环境
- 应用：seckill-service (Spring Boot 3.2.0, Java 17, 单实例)
- 中间件：MySQL 8.4 + Redis 8.10 + RabbitMQ 4.3.5 (Homebrew, 本地运行)
- 压测工具：Python 多进程并发
- 监控：Spring Boot Actuator + Micrometer 指标
- 日期：2026-08-19

---

## 场景一：Redis 宕机

### 注入方式
```bash
brew services stop redis
```

### 时间线

| 时间 | 事件 | 表现 |
|------|------|------|
| T+0s | Redis 停止 | 秒杀接口 500 错误 |
| T+0s | 登录接口 | 失败（JWT 黑名单依赖 Redis） |
| T+13s | 恢复 Redis | `brew services start redis` |
| T+17s | 系统恢复 | 秒杀接口正常响应 |

### 实际记录

| 指标 | 故障前 | 故障中 | 恢复后 |
|------|--------|--------|--------|
| 秒杀接口 | 秒杀成功 | 500/超时 | 正常 |
| 登录接口 | JWT Token | 失败 | 正常 |
| 恢复时间 | - | - | 4 秒 |

### 改进措施
- [x] 秒杀核心路径走 Redis Lua，Redis 宕机期间不可用——符合预期
- [ ] 可增加 Caffeine L1 缓存兜底，热点优惠券 Redis 宕机时仍可查询

---

## 场景二：MQ 积压

### 注入方式
```bash
rabbitmqctl stop_app   # 停止 RabbitMQ 应用
rabbitmqctl start_app  # 恢复
```

### 时间线

| 时间 | 事件 | 表现 |
|------|------|------|
| T+0s | MQ 停止 | RabbitMQ 不可达 |
| T+0s | 秒杀测试 | 秒杀正常（Redis 扣库存先行，MQ 后置） |
| T+2s | MQ 恢复 | 队列重建连接 |

### 实际记录

| 指标 | 故障前 | 故障中 | 恢复后 |
|------|--------|--------|--------|
| 秒杀可用性 | 正常 | 正常（Redis 路径） | 正常 |
| MQ 队列 | 0 条消息 | 发送失败（静默） | 0 条积压 |
| 订单创建 | 正常 | 延迟（补发） | 正常 |
| 死信队列 | 0 | 0 | 0 |

### 关键发现
秒杀接口在 MQ 不可用时仍然返回成功——因为架构是 **先 Redis 扣库存，后 MQ 发消息**。MQ 只用于异步创建订单，不影响秒杀主路径的可用性。

---

## 场景三：DB 慢查询

### 注入方式
```sql
-- 注入 5 个并发 SLEEP(10) 慢查询，占用 HikariCP 连接池
SELECT SLEEP(10);
```

### 时间线

| 时间 | 事件 | 表现 |
|------|------|------|
| T+0s | 5个慢查询注入 | 5/20 连接被占用 |
| T+0~10s | 慢查询期间 | 连接池仍有 15 个可用连接 |
| T+10s | 慢查询结束 | 连接释放 |

### 实际记录

| 指标 | 故障前 | 故障中 | 恢复后 |
|------|--------|--------|--------|
| 秒杀 RT | ~43ms | 84ms | 正常 |
| 登录 RT | ~50ms | 54ms | 正常 |
| 活跃连接 | 1-2 | 5 | 1-2 |

### 关键发现
秒杀核心路径不碰 MySQL——**Redis Lua 脚本完成扣库存**，MySQL 只用于异步订单落库。即使 DB 连接池被部分占满，秒杀 RT 仅从 43ms 涨到 84ms（Redis 未受影响）。

---

## 场景四：热点自动发现

### 测试方式
30 进程 × 200 请求并发秒杀（Python 多进程），观察 HotKeyDetector 行为。

### 结果

| 指标 | 值 |
|------|-----|
| 总请求 | 200 |
| QPS | 203 req/s |
| Caffeine 命中率 | 100% |
| 热点 Key 数量 | 0（200 请求 / 60s 窗口 = 3.3 QPS < 100 阈值） |

### 说明
热点阈值设为 QPS > 100（60s 滑动窗口）。当前压测 200 请求分散在 60s 窗口内，未达阈值。在秒杀活动高峰期（大量用户抢同一优惠券），该机制会自动触发 Caffeine 本地缓存升级。

---

## 总结

| 场景 | 恢复时间 | 秒杀可用性 | 关键发现 |
|------|---------|-----------|---------|
| Redis 宕机 | 4s | ❌ 不可用 | Redis 是秒杀唯一数据源，L1 缓存可做兜底 |
| MQ 积压 | 2s | ✅ 正常 | 先扣库存后发 MQ 架构保证核心路径 |
| DB 慢查询 | 10s | ✅ 正常 | 秒杀不碰 MySQL，连接池压力不影响主路径 |
| 热点发现 | - | ✅ | 阈值需根据实际流量调优 |

### 核心发现
1. 秒杀主路径经过 Redis 而非 MySQL，DB 慢查询不影响秒杀 RT
2. MQ 后置设计意味着即使 MQ 挂掉，秒杀核心功能不受影响
3. Redis 是单点风险——后续可加 Caffeine L1 缓存兜底热数据
4. HotKeyDetector 环形缓冲区工作正常，阈值需要根据实际活动流量调整
